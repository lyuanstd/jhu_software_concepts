[pytest]
pythonpath = src
testpaths = tests
addopts = --cov=src --cov-report=term-missing --cov-fail-under=100

markers =
    web: Flask page rendering tests
    buttons: Button and selector tests
    analysis: Analysis formatting tests
    db: Database insertion and idempotency tests
    integration: End-to-end integration tests